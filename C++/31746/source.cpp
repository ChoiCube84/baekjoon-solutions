#include <bits/extc++.h>

using namespace __gnu_pbds;
using namespace std;

int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	int N;
	cin >> N;

	if (N % 2 == 0) {
		cout << "SciComLove";
	}
	else {
		cout << "evoLmoCicS";
	}

	return 0;
}