#include <bits/extc++.h>

using namespace __gnu_pbds;
using namespace std;

using ll = long long int;
using ull = unsigned long long int;

using pll = pair<ll, ll>;

int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	
	// int u, v, k, t;
	// cin >> u >> v;
	
	// for (k=0; (u % 2 == 0) && (v % 2 == 0); k++) {
	// 	u /= 2; 
	// 	v /= 2;
	// } // k는 u와 v의 최대공약수의 2^n 성분
	
	// if (u % 2 == 0) {
	// 	t = -v; 
	// }
	// else {
	// 	t = u / 2;
	// }
	
	// while (t != 0) {
	// 	while (t % 2 == 0) {
	// 		t /= 2;
	// 	}
		
	// 	if (t > 0) {
	// 		u = t; 
	// 	}
	// 	else {
	// 		v = -t;
	// 	}
		
	// 	t = u - v;
	// }
	
	// while (k-- > 0) {
	// 	u *= 2; 
	// }
	
	// cout << u;
	
	return 0;
}
